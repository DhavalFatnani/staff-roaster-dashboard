/**
 * pages.cursor
 * 
 * Purpose: Main page components for the Staff Roster Dashboard
 * 
 * Run notes:
 * - These are React page components using Next.js-style routing
 * - Import components from './components.cursor'
 * - Use React Router or Next.js routing
 * - Replace API calls with your actual API client
 * - Add authentication checks and redirects
 */

import React, { useState, useEffect } from 'react';
import {
  User,
  Role,
  Roster,
  Permission,
  DeleteUserResult
} from './schema';
import {
  UserForm,
  StaffCard,
  BulkImportModal,
  DeleteUserConfirmModal,
  RoleForm,
  PermissionMatrix,
  ExportModal,
  ShareModal
} from './components';

// ============================================================================
// User Management Page
// ============================================================================

export function UserManagementPage() {
  const [users, setUsers] = useState<(User & { role?: Role })[]>([]);
  const [roles, setRoles] = useState<Role[]>([]);
  const [loading, setLoading] = useState(true);
  const [showUserForm, setShowUserForm] = useState(false);
  const [showBulkImport, setShowBulkImport] = useState(false);
  const [showDeleteConfirm, setShowDeleteConfirm] = useState(false);
  const [selectedUser, setSelectedUser] = useState<User | null>(null);
  const [deleteResult, setDeleteResult] = useState<DeleteUserResult | null>(null);
  const [searchTerm, setSearchTerm] = useState('');
  const [filterRole, setFilterRole] = useState<string>('');

  // TODO: Replace with actual API call
  useEffect(() => {
    // fetchUsers();
    // fetchRoles();
    setLoading(false);
  }, []);

  const handleCreateUser = async (data: any) => {
    // TODO: API call to create user
    // const newUser = await api.createUser(data);
    // setUsers(prev => [...prev, newUser]);
    setShowUserForm(false);
  };

  const handleUpdateUser = async (data: any) => {
    if (!selectedUser) return;
    // TODO: API call to update user
    // const updatedUser = await api.updateUser(selectedUser.id, data);
    // setUsers(prev => prev.map(u => u.id === updatedUser.id ? updatedUser : u));
    setShowUserForm(false);
    setSelectedUser(null);
  };

  const handleDeleteUser = async (user: User) => {
    // TODO: Check delete permissions and get impact
    // const result = await api.checkDeleteUser(user.id);
    // setDeleteResult(result);
    setSelectedUser(user);
    setShowDeleteConfirm(true);
  };

  const confirmDelete = async (reassignTo?: string, confirmVacancy?: boolean) => {
    if (!selectedUser) return;
    // TODO: API call to delete user
    // await api.deleteUser(selectedUser.id, { reassignTo, confirmVacancy });
    // setUsers(prev => prev.filter(u => u.id !== selectedUser.id));
    setShowDeleteConfirm(false);
    setSelectedUser(null);
    setDeleteResult(null);
  };

  const handleBulkImport = async (csvText: string) => {
    // TODO: Parse CSV and call bulk import API
    // await api.bulkImportUsers({ csvText });
    // fetchUsers();
  };

  const filteredUsers = users.filter(user => {
    const matchesSearch = 
      user.firstName.toLowerCase().includes(searchTerm.toLowerCase()) ||
      user.lastName.toLowerCase().includes(searchTerm.toLowerCase()) ||
      user.employeeId.toLowerCase().includes(searchTerm.toLowerCase());
    const matchesRole = !filterRole || user.roleId === filterRole;
    return matchesSearch && matchesRole;
  });

  // TODO: Get current user permissions
  const canEdit = true; // Check permission
  const canDelete = true; // Check permission
  const canCreate = true; // Check permission

  if (loading) {
    return <div className="p-8">Loading...</div>;
  }

  return (
    <div className="p-8">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-3xl font-bold">User Management</h1>
        <div className="flex gap-3">
          <button
            onClick={() => setShowBulkImport(true)}
            className="px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700"
          >
            Bulk Import
          </button>
          {canCreate && (
            <button
              onClick={() => {
                setSelectedUser(null);
                setShowUserForm(true);
              }}
              className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700"
            >
              + Add User
            </button>
          )}
        </div>
      </div>

      <div className="mb-6 flex gap-4">
        <input
          type="text"
          placeholder="Search users..."
          value={searchTerm}
          onChange={(e) => setSearchTerm(e.target.value)}
          className="flex-1 px-4 py-2 border rounded-md"
        />
        <select
          value={filterRole}
          onChange={(e) => setFilterRole(e.target.value)}
          className="px-4 py-2 border rounded-md"
        >
          <option value="">All Roles</option>
          {roles.map(role => (
            <option key={role.id} value={role.id}>{role.name}</option>
          ))}
        </select>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {filteredUsers.map(user => (
          <StaffCard
            key={user.id}
            user={user}
            onEdit={(u) => {
              setSelectedUser(u);
              setShowUserForm(true);
            }}
            onDelete={handleDeleteUser}
            canEdit={canEdit}
            canDelete={canDelete}
          />
        ))}
      </div>

      {showUserForm && (
        <UserForm
          user={selectedUser || undefined}
          roles={roles}
          onSubmit={selectedUser ? handleUpdateUser : handleCreateUser}
          onCancel={() => {
            setShowUserForm(false);
            setSelectedUser(null);
          }}
          isOpen={showUserForm}
        />
      )}

      {showBulkImport && (
        <BulkImportModal
          isOpen={showBulkImport}
          onClose={() => setShowBulkImport(false)}
          onImport={handleBulkImport}
        />
      )}

      {showDeleteConfirm && selectedUser && (
        <DeleteUserConfirmModal
          isOpen={showDeleteConfirm}
          user={selectedUser}
          deleteResult={deleteResult}
          onConfirm={confirmDelete}
          onCancel={() => {
            setShowDeleteConfirm(false);
            setSelectedUser(null);
            setDeleteResult(null);
          }}
        />
      )}
    </div>
  );
}

// ============================================================================
// Role Management Page
// ============================================================================

export function RoleManagementPage() {
  const [roles, setRoles] = useState<Role[]>([]);
  const [loading, setLoading] = useState(true);
  const [showRoleForm, setShowRoleForm] = useState(false);
  const [showPermissionMatrix, setShowPermissionMatrix] = useState(false);
  const [selectedRole, setSelectedRole] = useState<Role | null>(null);

  useEffect(() => {
    // TODO: Fetch roles
    // fetchRoles();
    setLoading(false);
  }, []);

  const handleCreateRole = async (data: any) => {
    // TODO: API call
    // const newRole = await api.createRole(data);
    // setRoles(prev => [...prev, newRole]);
    setShowRoleForm(false);
  };

  const handleUpdateRole = async (data: any) => {
    if (!selectedRole) return;
    // TODO: API call
    // const updatedRole = await api.updateRole(selectedRole.id, data);
    // setRoles(prev => prev.map(r => r.id === updatedRole.id ? updatedRole : r));
    setShowRoleForm(false);
    setSelectedRole(null);
  };

  const handleDeleteRole = async (role: Role) => {
    if (!window.confirm(`Delete role "${role.name}"? This action cannot be undone.`)) {
      return;
    }
    // TODO: API call with confirmation
    // await api.deleteRole(role.id);
    // setRoles(prev => prev.filter(r => r.id !== role.id));
  };

  if (loading) {
    return <div className="p-8">Loading...</div>;
  }

  return (
    <div className="p-8">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-3xl font-bold">Role Management</h1>
        <div className="flex gap-3">
          <button
            onClick={() => setShowPermissionMatrix(true)}
            className="px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700"
          >
            View Permission Matrix
          </button>
          <button
            onClick={() => {
              setSelectedRole(null);
              setShowRoleForm(true);
            }}
            className="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700"
          >
            + Create Role
          </button>
        </div>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {roles.map(role => (
          <div key={role.id} className="bg-white rounded-lg shadow p-4">
            <div className="flex justify-between items-start mb-2">
              <div>
                <h3 className="font-semibold text-lg">{role.name}</h3>
                {role.description && (
                  <p className="text-sm text-gray-600 mt-1">{role.description}</p>
                )}
              </div>
              {!role.isSystemRole && (
                <button
                  onClick={() => handleDeleteRole(role)}
                  className="text-red-600 hover:text-red-800 text-sm"
                >
                  Delete
                </button>
              )}
            </div>
            <div className="mt-3">
              <p className="text-xs text-gray-500">
                {role.permissions.length} permission(s)
              </p>
              <button
                onClick={() => {
                  setSelectedRole(role);
                  setShowRoleForm(true);
                }}
                className="mt-2 px-3 py-1 text-sm bg-blue-100 text-blue-700 rounded hover:bg-blue-200"
              >
                Edit
              </button>
            </div>
          </div>
        ))}
      </div>

      {showRoleForm && (
        <RoleForm
          role={selectedRole || undefined}
          onSubmit={selectedRole ? handleUpdateRole : handleCreateRole}
          onCancel={() => {
            setShowRoleForm(false);
            setSelectedRole(null);
          }}
          isOpen={showRoleForm}
        />
      )}

      {showPermissionMatrix && (
        <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div className="bg-white rounded-lg shadow-xl w-full max-w-6xl max-h-[90vh] overflow-y-auto p-6">
            <div className="flex justify-between items-center mb-4">
              <h2 className="text-2xl font-bold">Permission Matrix</h2>
              <button
                onClick={() => setShowPermissionMatrix(false)}
                className="px-4 py-2 border rounded-md hover:bg-gray-50"
              >
                Close
              </button>
            </div>
            <PermissionMatrix roles={roles} />
          </div>
        </div>
      )}
    </div>
  );
}

// ============================================================================
// Roster Builder Page
// ============================================================================

export function RosterBuilderPage() {
  const [roster, setRoster] = useState<Roster | null>(null);
  const [users, setUsers] = useState<User[]>([]);
  const [selectedDate, setSelectedDate] = useState(new Date().toISOString().split('T')[0]);
  const [selectedShift, setSelectedShift] = useState<'morning' | 'evening'>('morning');
  const [showExport, setShowExport] = useState(false);
  const [showShare, setShowShare] = useState(false);

  useEffect(() => {
    // TODO: Load roster for selected date/shift
    // loadRoster(selectedDate, selectedShift);
  }, [selectedDate, selectedShift]);

  const handleAssignUser = (slotId: string, userId: string) => {
    // TODO: Update roster slot
    // updateRosterSlot(slotId, userId);
  };

  const handlePublish = async () => {
    // TODO: Validate and publish roster
    // await api.publishRoster(roster.id);
  };

  const handleExport = async (format: 'csv' | 'pdf', options: any) => {
    // TODO: Call export API
    // await api.exportRosters({ format, ...options });
  };

  const handleShare = async (method: 'email' | 'slack', recipients: string[], message?: string) => {
    // TODO: Call share API
    // await api.shareRosters({ method, recipients, message, rosterIds: [roster.id] });
  };

  return (
    <div className="p-8">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-3xl font-bold">Roster Builder</h1>
        <div className="flex gap-3">
          <button
            onClick={() => setShowShare(true)}
            className="px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700"
          >
            Share
          </button>
          <button
            onClick={() => setShowExport(true)}
            className="px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700"
          >
            Export
          </button>
          <button
            onClick={handlePublish}
            className="px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700"
          >
            Publish Roster
          </button>
        </div>
      </div>

      <div className="mb-6 flex gap-4">
        <input
          type="date"
          value={selectedDate}
          onChange={(e) => setSelectedDate(e.target.value)}
          className="px-4 py-2 border rounded-md"
        />
        <select
          value={selectedShift}
          onChange={(e) => setSelectedShift(e.target.value as 'morning' | 'evening')}
          className="px-4 py-2 border rounded-md"
        >
          <option value="morning">Morning Shift</option>
          <option value="evening">Evening Shift</option>
        </select>
      </div>

      {roster && (
        <div className="space-y-4">
          {/* Roster slots and assignment UI */}
          <div className="bg-white rounded-lg shadow p-6">
            <h2 className="text-xl font-semibold mb-4">
              {selectedDate} - {selectedShift} Shift
            </h2>
            {/* TODO: Render roster slots */}
          </div>
        </div>
      )}

      {showExport && (
        <ExportModal
          isOpen={showExport}
          onClose={() => setShowExport(false)}
          onExport={handleExport}
        />
      )}

      {showShare && (
        <ShareModal
          isOpen={showShare}
          onClose={() => setShowShare(false)}
          onShare={handleShare}
        />
      )}
    </div>
  );
}

// ============================================================================
// Login Page
// ============================================================================

export function LoginPage() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  const handleLogin = async (e: React.FormEvent) => {
    e.preventDefault();
    setError(null);
    setLoading(true);

    try {
      // TODO: API call to authenticate
      // const user = await api.login({ email, password });
      // Redirect to dashboard
    } catch (err: any) {
      setError(err.message || 'Login failed');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <div className="bg-white rounded-lg shadow-xl p-8 w-full max-w-md">
        <h1 className="text-3xl font-bold mb-6 text-center">Staff Roster Dashboard</h1>
        <form onSubmit={handleLogin} className="space-y-4">
          <div>
            <label className="block text-sm font-medium mb-1">Email</label>
            <input
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full px-3 py-2 border rounded-md"
              required
            />
          </div>
          <div>
            <label className="block text-sm font-medium mb-1">Password</label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              className="w-full px-3 py-2 border rounded-md"
              required
            />
          </div>
          {error && (
            <div className="text-red-600 text-sm">{error}</div>
          )}
          <button
            type="submit"
            disabled={loading}
            className="w-full px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 disabled:opacity-50"
          >
            {loading ? 'Logging in...' : 'Login'}
          </button>
        </form>
      </div>
    </div>
  );
}

// ============================================================================
// Dashboard Layout (with navigation)
// ============================================================================

export function DashboardLayout({ children }: { children: React.ReactNode }) {
  const [currentUser, setCurrentUser] = useState<User | null>(null);

  useEffect(() => {
    // TODO: Get current user
    // fetchCurrentUser();
  }, []);

  return (
    <div className="min-h-screen bg-gray-50">
      <nav className="bg-white shadow-sm border-b">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between h-16">
            <div className="flex items-center">
              <h1 className="text-xl font-bold">Staff Roster Dashboard</h1>
            </div>
            <div className="flex items-center gap-4">
              <a href="/users" className="px-3 py-2 text-sm hover:bg-gray-100 rounded">
                Users
              </a>
              <a href="/roles" className="px-3 py-2 text-sm hover:bg-gray-100 rounded">
                Roles
              </a>
              <a href="/roster" className="px-3 py-2 text-sm hover:bg-gray-100 rounded">
                Roster
              </a>
              {currentUser && (
                <span className="text-sm text-gray-600">
                  {currentUser.firstName} {currentUser.lastName}
                </span>
              )}
            </div>
          </div>
        </div>
      </nav>
      <main>{children}</main>
    </div>
  );
}
